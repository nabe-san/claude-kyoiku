import { defineConfig } from 'astro/config';

// Vercel adapter: uncomment below when deploying
// import vercel from '@astrojs/vercel';
// adapter: vercel(),

export default defineConfig({
  output: 'static',
});
